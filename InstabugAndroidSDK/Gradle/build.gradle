apply plugin: 'java'

repositories {
  mavenCentral()
  maven { url "https://maven.google.com" }
}

dependencies {
  compile 'com.instabug.library:instabug:4+'
}

task downloadAndCopyLibs(type: Copy) {
  from configurations.runtime
  into 'build/lib'
}
